#include <iostream>
int main() {
long long suma = 0;
for (int i = 1; i <= 100; ++i) {
suma += (long long)i * i;
}
std::cout << "La suma de los cuadrados de los primeros 100 
numeros es: " << suma << std::endl;
return 0;
}

